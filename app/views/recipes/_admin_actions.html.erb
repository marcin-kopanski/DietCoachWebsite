<% if logged_in?(:site_admin) %>
   <nav class="breadcrumb blogcrumb">
      <%= link_to fa_icon('pencil-square-o'), edit_recipe_path(recipe) %>
      <%= link_to fa_icon('trash'), recipe, method: :delete, data: { confirm: 'Are you sure?' } %>
   </nav>
<% end %>
