<%= render "shared/blog_header" %>

<div class="bg-light py-3">
  <div class="container">
    <div class="row">
      <div class="col-12 text-center">
        <%= paginate @blogs %>
      </div>
    </div>

    <%= render @blogs %>

    <div class="row">
      <div class="col-12 text-center">
        <%= paginate @blogs %>
      </div>
    </div>
  </div>
</div>
